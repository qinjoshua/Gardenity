<!-- <div id="theMap">
    <img class="plot" src="/images/grass.jpg" alt="Workplace" usemap="#workmap">
</div> -->
<div id="theMap">
    <img class="plot" src="/images/grass.jpg" alt="Workplace" usemap="#workmap">
    <map name="workmap">
        <area shape="rect" coords="34,44,270,350" class="window" alt="Computer">
    </map>
</div>


<!-- The Modal -->
<div id="myModal" class="modal">

  <!-- Modal content -->
  <div class="modal-content">
    <div class="modal-header">
      <span class="close">&times;</span>
      <h2 style="text-align:left">Plot Name</h2>
    </div>
    <div class="modal-body">
      <div class="row">
        <div class="left">
            <h3 >Plants</h3>
            <div id="plantCards"></div>
        </div>
        <div class="middle">
            <h2 style="color:white">Tasks</h2>
            <div id="taskCards"></div>
        </div>
        <div class="right">
            <h3>People</h3>
            <div id="peopleCards"></div>
        </div>
      </div>
    </div>
  </div>

</div>

<script>
    const templateArea = (dataEntry) => `<area shape="rect" coords="${dataEntry}" class="window">`;


    const setupPlots = () => {
        body = document.getElementById("theMap");

        data = ["34,44,270,350"]
        data = ["34,200,270,500"]
        const map = document.createElement('map');
        for (let i = 0; i < data.length; i++){
            createArea(map, data[i])
        }

        body.appendChild(map);
    }

    const createArea = (target, data) => {
        const html = templateArea(data);
        target.insertAdjacentHTML('beforeend', html);
    }

    const templateContainer = (dataEntry) => `<div class="card"><div class="container">${dataEntry.column1}</div></div>`;


    const setupEachInfo = (element, data) => {
        body = document.getElementById(element);

        const div = document.createElement('div');
        for (let i = 0; i < data.length; i++){
            createContainer(div, data[i])
        }

        body.appendChild(div);
    }

    const setupInfo = () => {
        var dataPlants = [
            { column1: 'Tomatos'},
            { column1: 'Squash'}
        ];
        var dataTask = [
            { column1: 'Water 11/12'},
            { column1: 'Water 11/28'},
            { column1: 'Water 11/29'},
            { column1: 'Water 12/29'},
            { column1: 'Water 12/29'},
            { column1: 'Water 12/29'},
            { column1: 'Water 12/29'},
            { column1: 'Water 12/29'}
        ];
        var dataPeople = [
            { column1: 'Bob'},
            { column1: 'Alice'}
        ];
        var cols = [{col: 'plantCards', data: dataPlants}, {col: 'taskCards', data: dataTask}, {col: 'peopleCards', data: dataPeople}]
        for (let i = 0; i < cols.length; i++){
            setupEachInfo(cols[i].col, cols[i].data)
        }
    }

    const createContainer = (target, data) => {
        const html = templateContainer(data);
        target.insertAdjacentHTML('beforeend', html);
    }

    const setup = () => {
        setupInfo()
        setupPlots()
    }

    window.addEventListener('load', setup);

    // Get the modal
    var modal = document.getElementById("myModal");

    // Get the <span> element that closes the modal
    var span = document.getElementsByClassName("close")[0];

    var btn = document.getElementsByClassName("window");

    // When the user clicks on <span> (x), close the modal
    for(let i = 0; i < btn.length; i++){
        btn[i].onclick = function() {
            console.log("test")
            modal.style.display = "block";
        }
    }

    // btn.on("click", function(e)){
    //     e.preventDefault();
    //     modal.style.display = "block";
    // }
    //});

    // When the user clicks the button, open the modal 
    function openModal () {
        console.log("test");
        modal.style.display = "block";
    }

    // When the user clicks on <span> (x), close the modal
    span.onclick = function() {
        console.log("test2");
        modal.style.display = "none";
    }

    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function(event) {
    if (event.target == modal) {
        modal.style.display = "none";
    }
    }
</script>